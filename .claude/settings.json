{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [[ \"$f\" =~ /app/|^app/|/tests/|^tests/ ]] && .venv/bin/python3 -m ruff check --fix \"$f\" && .venv/bin/python3 -m ruff format \"$f\" 2>/dev/null || true",
            "statusMessage": "Formatting with ruff..."
          }
        ]
      }
    ]
  }
}
